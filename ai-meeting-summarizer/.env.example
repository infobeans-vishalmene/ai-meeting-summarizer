# AWS region for Bedrock (no secrets here — credentials come from the
# standard boto3 credential chain: env vars, ~/.aws/credentials, or IAM role)
AWS_REGION=us-east-1

# Path to SQLite database file
DATABASE_PATH=sqlite.db
